import React from 'react';

// The previous base64 string was too large and crashed the application at runtime.
// This is now a placeholder. Consider loading the logo from a file in the public directory.
const BrknLogo: React.FC<React.SVGProps<HTMLImageElement>> = (props) => {
    // Returning null or a simple placeholder to avoid the crash.
    return null;
};

export default BrknLogo;